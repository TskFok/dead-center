# 发现与决定

## 需求
- 将「细旗空心菱形」改为从屏幕四周边缘开始，向屏幕中心点延伸。
- “整体尺寸”对所有预设统一改为目标屏幕百分比表达。
- 对「细旗空心菱形」，0% 时四个外端已经锚定目标屏幕四边；百分比越高，旗臂从边缘向中心延伸得越长。
- 对「细旗空心菱形」，100% 时四条旗臂尖端停在中心缺口／空心菱形外缘，不能覆盖中心标记。
- 对「细旗空心菱形」，0% 时仍保留约等于线宽的最短可见旗头，并锚定屏幕四边。
- 保持当前分支，不擅自新建分支。

## 调研发现
- 已读取适用的工作流程技能；当前处于项目现状检查阶段，尚未修改产品代码。
- 当前分支为 `main`，与 `origin/main` 同步；除本次新建的三个规划文件外，工作区无其他未提交改动。
- 最新提交 `67c0fb7 样式：细旗菱形改为贴边向心` 已完成“准星容器边缘向中心延伸”的第一版，因此本次重点很可能是把参照物从“准星容器”提升为“屏幕”，并把尺寸单位改成屏幕百分比。
- 已存在相关设计与实施计划：`docs/superpowers/specs/2026-07-13-fine-diamond-inward-edge-design.md`、`docs/superpowers/plans/2026-07-13-fine-diamond-inward-edge.md`。
- 主要实现和测试入口位于 `src/components/Crosshair.tsx`、`src/components/Crosshair.css`、`src/components/Crosshair.test.tsx`，设置入口位于 `src/SettingsApp.tsx` 及其测试。
- 当前 `Crosshair` 根元素始终把 `sizePx` 映射为等宽等高的像素值；设置面板“整体尺寸”范围为 12–192 px，配置协议字段名也是 `sizePx`。
- 最近的“贴边”只把 `fine-diamond` 的 `--flag-inset` 从 22% 改为 0%，因此旗臂贴的是 Crosshair 正方形容器边缘，并非显示器边缘。
- 覆盖层由独立 Tauri 窗口承载；检索结果表明窗口尺寸由后端 `OVERLAY_SIZE` 常量控制并在目标显示器中央定位，需继续确认它是否覆盖整屏。
- `MonitorInfo` 已把目标屏幕宽、高和缩放因子传给前端快照，因此有条件按实际目标屏幕计算百分比尺寸，或让覆盖层直接使用视口百分比。
- 当前“细旗空心菱形”旗臂已经是尖头朝中心，其他三种菱形方案仍按各自预设工作。
- 覆盖层实际固定为 `256 × 256` 逻辑像素，并按目标屏幕缩放因子计算物理位置后居中；它没有覆盖全屏。这正是当前旗臂无法从显示器边缘起步的根因。
- `.overlay-root` 已占满覆盖层视口，所以若后端把窗口改为目标显示器的完整逻辑宽高，前端即可用 `100vw × 100vh` 表达屏幕范围；非正方形屏幕要求 Crosshair 能使用独立宽、高，不能继续只用单一正方形 `sizePx`。
- 现有书面规格明确写的是“准星容器边缘”，因此用户的新要求是对上一版设计的范围纠正，而非单纯 CSS 微调。
- 配置协议前后端都持久化 `sizePx`，默认 32、范围 12–192；把“整体尺寸”改为百分比会涉及 TypeScript、Rust、设置 UI、配置迁移兼容和测试，不应只替换显示单位。
- 目标屏幕宽高来自物理像素，覆盖层窗口尺寸 API使用逻辑像素；全屏覆盖层必须按 `scale_factor` 换算宽高，并把位置设置为显示器物理原点。
- 用户确认普通预设以目标屏幕短边作为百分比基准，例如 1920×1080 屏幕的 10% 对应 108 px；这样保持圆形和正方形预设不变形。
- `update_visual` 会先在 Rust 端规范化配置、持久化，再向覆盖层发出视觉设置事件；因此百分比字段需要前后端同时改名并统一边界。
- 选择目标屏幕后当前只刷新覆盖层位置；全屏方案还必须同步刷新覆盖层尺寸，并让设置预览使用新目标屏幕的宽高比例。
- 当前配置文件只接受 `version: 1` 且要求完整反序列化为 `AppSettings`；若直接把 `sizePx` 改名为 `sizePercent`，旧配置会被当成损坏并重置。设计必须包含显式迁移，不能借用“损坏配置恢复”路径。
- 旧 `sizePx` 可用目标屏幕的逻辑短边换算为近似等视觉尺寸的百分比：`sizePx / (min(physicalWidth, physicalHeight) / scaleFactor) × 100`。
- 设置页预览区当前为固定高度矩形且居中渲染 Crosshair；可直接作为“目标屏幕缩略画布”的容器，普通预设按容器短边缩放，`fine-diamond` 填满容器并贴四边，无需重做设置页布局。
- 实施计划可分为四个可独立审阅的交付：跨前后端百分比配置与迁移、全屏覆盖层几何、细旗与预览画布渲染、文档及完整回归验证。
- 前端现有测试直接断言 `sizePx: 32`、12–192 滑杆和 `32px` 行内尺寸；这些是百分比迁移的明确 RED 测试入口。
- `Crosshair` 当前只接收 `VisualSettings`，适合继续保持纯渲染边界；渲染画布尺寸应由父容器提供，组件只消费百分比和 CSS 几何变量。
- Rust 配置迁移适合在 `config.rs` 提供版本 1 结构和纯迁移函数，在 `state.rs` 负责根据实际解析屏幕提供逻辑短边并立即保存版本 2；这样迁移算法可独立单测，存储职责不外泄。
- 全屏窗口几何适合由 `monitor.rs` 的纯函数返回物理原点与逻辑宽高，`overlay.rs` 只负责应用到 Tauri 窗口；现有两秒轮询和目标屏幕切换调用统一改为刷新完整几何。
- 前端可让 `.overlay-root` 和设置页内部预览画布成为尺寸容器，普通预设用短边容器单位，细旗预设铺满容器；这能让同一个 `Crosshair` 组件继续服务覆盖层与预览。
- 细旗进度可通过 CSS 变量表达为 `max(strokePx, calc(sizePercent / 2% - scaledClearancePx))`；其中中心净空取 10px 的旋转菱形半径上取整再加 `gapPx / 2`，在 100% 时精确停在中心外缘。

## 技术决定
| 决定 | 理由 |
|------|------|
| 先确认现有坐标与配置模型，再选择百分比换算位置 | 避免重复缩放或破坏已有序列化配置 |
| 第一阶段不修改产品代码 | brainstorming 技能要求先完成设计确认 |
| 需把“屏幕百分比”的适用范围问清楚 | 它可能是全局设置协议变更，也可能只是 `fine-diamond` 的专用渲染规则，两者影响范围明显不同 |
| 百分比字段对全部预设统一，但 `fine-diamond` 采用“从边缘向内延伸”的几何解释 | 用户已明确统一百分比单位，并明确细旗预设的 0% 边缘锚定与递增方向 |
| `fine-diamond` 的 100% 终点为中心缺口／空心菱形外缘 | 用户明确要求保留中心标记的可辨识度 |
| 普通预设按目标屏幕短边换算百分比 | 用户已确认；可保持正方形、圆形和十字预设的几何比例 |
| 保留旧配置并显式迁移 `sizePx` | 直接改字段会让现有用户配置被误判损坏；迁移可最大限度保持升级前视觉尺寸 |
| 0% 仍保留最短可见旗头 | 用户已确认；避免边缘锚点在零值时完全消失 |
| 实施按配置、窗口几何、前端几何、文档验证四个任务拆分 | 每个任务均有独立测试面和提交边界，且顺序依赖清晰 |
| 配置迁移与窗口几何均提取纯函数 | 便于先写失败测试并覆盖无显示器、高 DPI、负坐标和回退场景 |

## 遇到的问题
| 问题 | 处理 |
|------|------|
| 暂无 | — |

## 资源
- 项目根目录：`/Users/ushopal/workspace/myself/dead-center`
- 最近相关提交：`67c0fb7`、`3f0b621`、`c9152f0`

## 视觉/浏览器发现
- 暂无。

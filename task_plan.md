# 任务计划：调整「细旗空心菱形」布局

## 目标
将「细旗空心菱形」改为从屏幕四周边缘向中心点延伸，并让整体尺寸由屏幕百分比决定，同时保持现有项目行为稳定。

## 当前阶段
阶段 3

## 阶段

### 阶段 1：需求与现状确认
- [x] 定位实现、测试、文档与近期相关提交
- [x] 明确“屏幕百分比”和边缘起点的完整配置语义
- [x] 记录发现
- **状态：** 完成

### 阶段 2：设计与书面规格
- [x] 提出备选方案并获得用户确认
- [x] 写入设计文档并自检
- [x] 获得用户对书面规格的确认
- [x] 编写实施计划
- **状态：** 完成

### 阶段 3：测试驱动实现
- [x] 先写失败测试并确认失败原因
- [x] 完成最小实现
- [x] 重构且保持测试通过
- **状态：** 完成

### 阶段 4：验证与交付
- [x] 运行针对性测试与项目级验证
- [x] 检查差异与用户现有改动
- [x] 汇总交付内容
- **状态：** 完成（桌面视觉/交互清单待有权限的图形会话验收）

## 关键问题
1. 当前“细旗空心菱形”由哪些参数控制几何形状？
2. 百分比应相对屏幕宽、高分别计算，还是相对短边统一计算？
3. “从屏幕边缘四周开始”是否意味着四个顶点分别贴住上、右、下、左边缘？
4. “整体尺寸”改为百分比应影响所有预设，还是只在 `fine-diamond` 下解释为屏幕占比？
5. `fine-diamond` 的 100% 应让旗臂尖端停在中心缺口/菱形外缘，还是覆盖到精确中心点？（已确认：停在外缘）
6. 圆环、十字等保持正方形比例的预设，应以目标屏幕短边还是其他维度作为 100% 基准？

已确认：以目标屏幕短边为基准。
7. `fine-diamond` 在 0% 时应保留最短可见旗头，还是允许旗臂长度归零而不可见？（已确认：保留最短可见旗头）

## 已做决定
| 决定 | 理由 |
|------|------|
| 在当前分支修改 | 遵循用户提供的 AGENTS.md 工作约定 |
| 不在循环遍历中查询 SQL | 遵循用户提供的 AGENTS.md 工作约定；如本任务不涉及 SQL 则保持不触碰 |
| 所有预设统一持久化百分比尺寸 | 用户已确认统一单位 |
| 普通预设按目标屏幕短边换算；`fine-diamond` 外端锚定四边、百分比控制向内长度 | 同时满足几何比例与贴边向心语义 |
| 使用单个全屏透明覆盖层 | 用户选择方案 1；几何与多显示器同步最统一 |
| 预览画布匹配实际解析屏幕的逻辑宽高比 | 避免固定预览矩形让贴边形状或短边百分比失真 |
| 设计规格单独提交 | brainstorming 工作流要求先形成可审阅、可追溯的书面规格 |
| 实施计划采用四个任务及频繁中文提交 | 符合 writing-plans 的 TDD、可审阅交付和项目提交约定 |
| 全屏窗口应用物理宽高并同时保留逻辑尺寸计算 | 避免跨不同 DPI 屏幕移动时使用旧窗口缩放因子导致尺寸偏差 |
| 预览画布通过外层容器单位同时约束宽高 | 确保横屏、竖屏和窄窗口下都保持实际屏幕比例 |

## 遇到的错误
| 错误 | 尝试 | 处理 |
|------|------|------|
| 暂无 | 1 | — |

## 备注
- 所有面向用户的回复使用简体中文。
- 未经用户明确要求，不创建新分支。
